import { Inter, Permanent_Marker } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const cursive = Permanent_Marker({
  subsets: ["latin"],
  weight: "400",
});
